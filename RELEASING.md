Because I always forget how to publish new versions of this theme:

https://code.visualstudio.com/api/working-with-extensions/publishing-extension

1. Change version in package.json and create version commit+tag
2. Push commit+tag
3. `vsce publish` (get command with `npm i -g @vscode/vsce`)
